import 'package:flutter/material.dart';

import 'package:notekeeper/screens/homescreen.dart';

main() {
  runApp(const MaterialApp(
    title: 'UI',
    home: Notelist(),
    debugShowCheckedModeBanner: false,
  ));
}
